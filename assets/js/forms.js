const SOLAREX_FORM_ENDPOINT="https://forms.nanotech-solutions.com/solarex_forms/submit.php";
document.addEventListener('DOMContentLoaded',()=>{document.querySelectorAll('.site-form').forEach(form=>{form.addEventListener('submit',async e=>{e.preventDefault();const status=form.querySelector('.form-status');status.textContent='Submitting…';const data=Object.fromEntries(new FormData(form).entries());data.consent=!!form.querySelector('[name="consent"]:checked');try{const r=await fetch(SOLAREX_FORM_ENDPOINT,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});const j=await r.json().catch(()=>({ok:false}));if(!r.ok||!j.ok)throw new Error(j.message||'Submission endpoint unavailable.');status.textContent='Submission received.';status.style.color='var(--green)';form.reset();}catch(err){status.textContent='The form could not be submitted automatically. Please try again or contact SolarEX through the approved commercial route.';status.style.color='var(--gold)';}});});});
